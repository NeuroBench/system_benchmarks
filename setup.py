from setuptools import find_packages, setup

setup(
    name='system_track',
    packages=find_packages(),
    version='0.0.1',
    description='Benchmark code for the Neurobench project',
    author='petrut',
    license='',
)
